VITE_PDF_API_URL=http://localhost:8001

# Só para rodar este projeto standalone (npm run dev) sem o Front Host —
# um ModuleAccessToken válido para o módulo "pdf". Não usado em produção.
VITE_DEV_MODULE_TOKEN=
