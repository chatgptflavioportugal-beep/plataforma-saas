VITE_WHATSAPP_API_URL=http://localhost:8002

# Só para rodar este projeto standalone (npm run dev) sem o Front Host —
# um ModuleAccessToken válido para o módulo "whatsapp". Não usado em produção.
VITE_DEV_MODULE_TOKEN=
