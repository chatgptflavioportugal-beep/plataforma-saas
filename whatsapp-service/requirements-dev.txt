# Dependências extras para desenvolvimento/testes locais — NÃO instaladas na
# imagem de produção (Dockerfile instala apenas requirements.txt).
-r requirements.txt
httpx>=0.27.0
PyJWT>=2.8.0
pytest>=8.0.0
