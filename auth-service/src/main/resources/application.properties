# =============================================================
# Datasource — Supabase PostgreSQL (mesmo banco do backend-quarkus
# nesta fase da migração; separação de banco fica para uma fase futura)
# AUTH_DB_USERNAME/AUTH_DB_PASSWORD permitem apontar para uma role de least
# privilege dedicada (ver database/migrations/0051_least_privilege_roles.sql)
# sem quebrar quem ainda usa a credencial única compartilhada.
# =============================================================
quarkus.datasource.db-kind=postgresql
quarkus.datasource.jdbc.url=${QUARKUS_DATASOURCE_JDBC_URL}
quarkus.datasource.username=${AUTH_DB_USERNAME:${QUARKUS_DATASOURCE_USERNAME}}
quarkus.datasource.password=${AUTH_DB_PASSWORD:${QUARKUS_DATASOURCE_PASSWORD}}

quarkus.datasource.jdbc.min-size=2
quarkus.datasource.jdbc.max-size=10
quarkus.datasource.jdbc.acquisition-timeout=30

quarkus.hibernate-orm.database.generation=none

# =============================================================
# JWT — Supabase (ES256)
# =============================================================
mp.jwt.verify.publickey.location=${SUPABASE_JWT_JWKS_URL}
mp.jwt.verify.issuer=${SUPABASE_JWT_ISSUER}
mp.jwt.token.header=Authorization
mp.jwt.token.cookie=
smallrye.jwt.verify.algorithm=ES256

# =============================================================
# CORS
# =============================================================
quarkus.http.cors=true
quarkus.http.cors.origins=${CORS_ORIGINS:http://localhost:5100}
quarkus.http.cors.methods=GET,POST,PUT,DELETE,PATCH,OPTIONS
quarkus.http.cors.headers=Authorization,Content-Type,X-Tenant-ID,Accept

# Security headers
quarkus.http.header."X-Frame-Options".value=DENY
quarkus.http.header."X-Content-Type-Options".value=nosniff
quarkus.http.header."Strict-Transport-Security".value=max-age=31536000
quarkus.http.header."Referrer-Policy".value=strict-origin-when-cross-origin

# =============================================================
# OpenAPI / Swagger
# =============================================================
quarkus.smallrye-openapi.path=/q/openapi
quarkus.swagger-ui.always-include=true

# =============================================================
# Jackson — snake_case e datas como ISO string (compatível com frontend TypeScript)
# =============================================================
quarkus.jackson.property-naming-strategy=SNAKE_CASE
quarkus.jackson.write-dates-as-timestamps=false

# =============================================================
# REST Client — subscription-service (resolução de acesso a módulo:
# assinatura/plano/limites, usado por ModuleTokenResource)
# =============================================================
quarkus.rest-client.subscription-service-api.url=${SUBSCRIPTION_SERVICE_URL:http://localhost:8085}

# =============================================================
# Custom Tokens (ProfileAccessToken / ModuleAccessToken)
# Segredos distintos por tipo de token; devem ser IDÊNTICOS aos usados por
# quem valida esses tokens (usage-service, pdf-service, whatsapp-service).
# Sem default: o boot deve falhar se a env var não for injetada — nunca
# reintroduzir um valor literal aqui (já vazou uma vez, ver auditoria).
# =============================================================
app.token.profile-secret=${PROFILE_ACCESS_TOKEN_SECRET}
app.token.module-secret=${MODULE_ACCESS_TOKEN_SECRET}
app.token.profile-expiry-hours=${PROFILE_TOKEN_EXPIRY_HOURS:8}
app.token.module-expiry-minutes=${MODULE_TOKEN_EXPIRY_MINUTES:30}

# =============================================================
# App
# =============================================================
quarkus.http.port=8082
quarkus.log.level=INFO
quarkus.log.category."com.saas".level=DEBUG

%dev.quarkus.http.cors.origins=${CORS_ORIGINS:http://localhost:5100,http://localhost:5101,http://localhost:5102,http://localhost:5200}
%dev.quarkus.log.level=DEBUG
%dev.quarkus.swagger-ui.always-include=true

# Test — sobrescreve apenas o que é necessário
%test.quarkus.datasource.jdbc.url=${QUARKUS_DATASOURCE_JDBC_URL}
%test.quarkus.datasource.username=${QUARKUS_DATASOURCE_USERNAME}
%test.quarkus.datasource.password=${QUARKUS_DATASOURCE_PASSWORD}
